import React from "react";
import spinner from "../spinner.gif"

function Spinner() {
    return (
        <div className="container text-center ">

            <img src={spinner} alt="Cannot Found"/>

        </div>
    );
}

export default Spinner;